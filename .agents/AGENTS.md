# Reglas del Proyecto (Workspace Rules)

- **Ejecución manual de pruebas por el usuario**: Las pruebas (`npm run test:...`, `npx playwright test ...`) las ejecuta SIEMPRE el usuario en su terminal para probar. El asistente NUNCA debe ejecutar los comandos de prueba de forma automática o por su cuenta.
